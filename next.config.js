/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  nextConfig,
  images: {
    reactStrictMode: true,
    domains: ["images.unsplash.com"],
  },
};
